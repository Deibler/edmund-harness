# sessions/

Runtime session transcripts (`<session-uuid>.jsonl`), written by the harness.

Leave this empty — nothing here is authored by hand. It exists so the directory
is present on a fresh copy. `edmund sessions compact <key>` trims files here
when they grow large.
